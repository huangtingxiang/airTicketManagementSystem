import { PlaneTypePipe } from './plane-type.pipe';

describe('PlaneTypePipe', () => {
  it('create an instance', () => {
    const pipe = new PlaneTypePipe();
    expect(pipe).toBeTruthy();
  });
});
